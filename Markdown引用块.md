# 单级引用
区块引用通过在段落开头使用“>”接一个空格实现，eg：
> 区块引用
> 鲁迅曾经说过:
>
> 今天无事，主要打麻将！

简化写法，可只在第一行使用“>”，其余行自动包含其中，eg：
> 区块引用
鲁迅曾经说过:
今天无事，主要打麻将！

中间有空白行时，必须通过“>”衔接
> 区块引用
鲁迅曾经说过:
>
>今天无事，主要打麻将！

git test2！在featureTest2中增加

# 引用层级与嵌套
每多一个“>”引用就多一层，eg：
>>>>>>> 直接创建七级引用

在进行嵌套引用时，每一层在上一层的基础上增加一个“>”即可，eg：
> 最外层
>> 第一层嵌套
这也是第一层
>>> 第二层嵌套
这也是第二层

但同一段落中后续默认向当前最高级同化，eg：
>>> 这是一个三级的引用
后续换行直接输入也是三级
> 此处我想通过“> ”创建一个一级的引用，但失败！

> 只有通过新段落才能创建一级的引用！

# 区块中使用列表
区块中使用列表，语法如下：
> 区块中使用列表
> 1. 第一项
>    - 第1.1层
>      - 第二层
>        1. 第三层
>    - 第1.2层
> 2. 第二项

注意：区块中列表嵌套时，不可简化“>”,第一层嵌套至少需要在相对最外层前面加3个空格！

# 列表中使用区块
在列表中使用区块时，语法如下：
* 做饭
    > 番茄鸡蛋
    >> 番茄
    >> 鸡蛋
* 打麻将

注意：需要在>前面加4个空格！

# 引用块内的其它元素
## 包含标题

> ## 重要提醒 
>
> 请在操作前仔细阅读文档，避免数据丢失。

## 包含列表
> ### 注意事项
> 
> 1. 备份重要数据
> 2. 检查系统兼容性
> 3. 准备回滚方案
> 
> - 测试环境验证
> - 生产环境部署
> - 监控系统状态

## 包含代码
> 要运行这个脚本，请使用以下命令：
> 
> ```bash
> npm install
> npm start
> ```
> 
> 执行后会在 `http://localhost:3000` 看到结果。

## 包含图片和链接
> &#x1f4da; **推荐阅读**
> 
> 详细信息请参考 [官方文档](https://example.com)
> 
> ![示例图片](./images/example.png)
    
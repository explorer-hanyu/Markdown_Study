# 字体
Markdown一共支持粗体、斜体、粗斜体三种字体效果：
可通过两个“*”或“_”的方式来表示粗体，如下所示：
**粗体语法1**
__粗体语法2__

可用给一个“*”或“_”的方式在标识斜体，如下所示：
*斜体语法*
_斜体语法_

可通过三个“*”的方式标识粗斜体
***粗斜体语法***
*__试试这样呢__*，
__*这样可以吗？*__
___那这样呢？___
由此可见粗斜体就是粗体和协议混合使用，但是通常由三个“*”或“_”标识最方便

三种字体混合使用的效果展示：**粗体**、*斜体*、***粗斜体***。

# 分割线
MAekdown创建可通过“*”、“-”、"_"三种字符创建分割线，在某一行中输入三个以上同一种符号，行内可插入空格但不能有其他字符：
*号创建分割线示例：
***
-创建分割线示例：
- - -
_创建分割线示例：
 _ _   _ 

# 下划线
下划线可通过HTML中的<u>标签开始，结束用</u>表示即可，参照本条内容

# 空格
|实体|宽度|实体|宽度|
|---|---|---|---|
|`&nbsp;`|半角（约 1/2 汉字）|`&ensp;`|半角（稳定 1/2 汉字）|
|`&emsp;`|全角（1 个汉字宽）|`&thinsp;`|窄空格（约 1/5 汉字）|

# 删除线
删除线可通过两个波浪线前后标记即可，如下所示：
今天早上吃~~鸡腿，不对，是~~小面。

# 脚注
脚注主要用于对问文本的补充说明，通过[^要注明的文本]表示，如下所示：
小杯[^小杯]属于小包。

[^小杯]:ZLD对HY的专属称呼！

# 行内代码标记
行内代码标记主要用于强调，通过前后各一个反引号包围要标记的文本：
使用 `git commit` 提交代码；
出门请先检查`燃气`！

当文本本身包含反引号时，使用两个反引号间隔一个空格包围，：
请提交`` `code` ``

# 文本高亮
文本高亮不是Markdowm标准语法，但许多扩展支持。
方式1：通过前后各两个=包围文本进行高亮：
回家==吃饭==

方式2：通过HTML语法高亮：
上床<mark>睡觉</mark>

# 段落与换行
在Markdown中段落由一个或多个连续的文本行组成，段落之间由一个空行分隔，段落不应该用空格或制表符缩进。如下所示：

这是第一段。可以包含连续多个句子。也可以只有一句话。
连续的行也算是同一个段落。

这是合理的第二段。与第一段之间由一个空行分隔。

       这是不合理的第三段，前面不允许出现空格或制表符。

强制段落换行方法：
1.通过HTML标签换行：
<u>第一行内容<br>第二行内容[^<br>]
第三行内容</u>

[^<br>]:下划线影响域可见，只能换行，并不能开启新的段落！